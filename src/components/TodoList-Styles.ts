import styled from "styled-components";

const TodoList = styled.div`
  background-color: rgba(190, 190, 234, 0.66);
  border: 1px solid blueviolet;
  border-radius: 5px;
  padding: 15px;
`


export const S = {
     TodoList
}